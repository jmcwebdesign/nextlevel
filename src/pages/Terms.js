import { Terms } from '../containers';
import { Navbar, Footer } from '../components';

const TermsPage = () => {
    return (
        <div>
            <Navbar />
            <Terms />
            <Footer />
        </div>
    );
};

export default TermsPage